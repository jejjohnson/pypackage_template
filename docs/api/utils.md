# Utilities

Validators, the generic sliding [`Window`][mypackage.Window], and a timing
context manager. These are public because user-defined transforms need the
same validation behaviour as the built-in ones.

::: mypackage.utils
